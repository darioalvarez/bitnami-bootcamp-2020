apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  namespace: s3d1
  labels:
    app: mariadb
    tier: backend
spec:
  ports:
  - port: 3306
    targetPort: mariadb-server
  selector:
    app: mariadb
    tier: backend
  type: ClusterIP
